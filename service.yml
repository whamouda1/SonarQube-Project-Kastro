apiVersion: v1
kind: Service
metadata:
  name: spotify-app-service
  labels:
    app: spotify-app
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 3000  # Replace with the port your app runs on
  selector:
    app: spotify-app
